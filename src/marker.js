{
  /* {crimes.map(crime => (
          <Marker 
          key={crime.id} 
          lat={crime.latitude} 
          lng={crime.longitude}>
            <button className="crime-marker">
              <img src="/custody.svg" alt="crime doesn't pay" />
            </button>
          </Marker> */
}
